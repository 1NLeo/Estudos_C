#include <stdio.h>

int main () {

    int num;
    int size;

    scanf ("%d", &size);

    for (int i = 0; i < size; i++) {
        num = 0;
        scanf ("%d", &num);
        printf ("%d\n", 67);
    }

 

    return 0;
}