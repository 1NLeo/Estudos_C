#include <stdio.h>

int main () {

    int num;
    int siz;

    scanf ("%d", &siz);

    for (int i = 0; i < siz; i++) {
        num = 0;
        scanf ("%d", &num);
        printf ("%d\n", 67);
    }

 

    return 0;
}