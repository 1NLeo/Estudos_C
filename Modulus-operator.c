#include <stdio.h>

int main () {

    int x = 19, y = 3;

    int modulus = x % y;

    printf ("Remainder: %d", modulus); // The result is 1 because 6 * 3 = 18 and 19 - 18 = 1, so the remainder is equal to 1. 

    return 0;
}